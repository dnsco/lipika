---
type: file_exists
path: "workstreams/*/reference/*.md"
---

# R2b — a reference trace is created under the thread's `reference/`

R2 clause 3, moved from a judge to a free mechanical check: the trace is written to
`workstreams/<thread>/reference/`, with the thread's own folder as its home.

Only files created during the run count, so this proves **creation** and says nothing about editing.
Provenance and the reason this split from R2: `design/eval-cases/external-references.md`.

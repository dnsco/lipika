---
type: file_exists
path: "workstreams/*/reference/*.md"
---

# a reference trace is created under the thread's `reference/`

`trace-names-are-dated` clause 3, moved from a judge to a free mechanical check: the trace is written to
`workstreams/<thread>/reference/`, with the thread's own folder as its home.

Only files created during the run count, so this proves **creation** and says nothing about editing.
Provenance and the reason this split from `trace-names-are-dated`: `design/eval-cases/external-references.md`.

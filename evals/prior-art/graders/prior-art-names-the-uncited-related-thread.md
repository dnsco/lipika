---
type: regex
target: trace
pattern: '/orientation/[^"]*\.md","content":"(?:[^"\\]|\\.)*## Prior art(?:[^"\\]|\\.)*why-is-the-cache-slow'
---

# prior art names the related thread that nothing cites

The archived cache-latency thread found that eviction sweeps were the cost. Nothing links to it,
and it is archived, so only a search reaches it. The new orientation's `## Prior art` must point at
it. The parent is named in the prompt, so pointing at the parent alone proves nothing.

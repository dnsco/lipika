---
type: regex
target: trace
pattern: 'what-does-the-queue-guarantee(?:(?!\\n).)*\|(?:(?!\\n).)*still live'
---

A ledger row names `what-does-the-queue-guarantee` and, on the same line, `still live`.
